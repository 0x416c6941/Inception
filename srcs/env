# For volumes.
# It's better NOT to use "${USER}" to prevent accidental removal
# of user data in "purge" recipe in Makefile.
LOGIN='asagymba'

# Docker volumes.
WP_VOL_PATH="/home/${LOGIN}/data/wordpress"
DB_VOL_PATH="/home/${LOGIN}/data/mariadb"
